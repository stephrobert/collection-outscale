# Décisions humaines sur le produit `listener` (tag `Listener`).

operations:

  listener.v1.ListenerRule.ReadListenerRules:
    expose: false
    reason: >-
      feint 0.12.1 ne sert aucune des quatre opérations de règle d'écoute
      (CreateListenerRule, ReadListenerRules, UpdateListenerRule,
      DeleteListenerRule ; mesuré sur son artefact de couverture, le
      5 septembre 2026) : sans règle, la lecture n'a rien à rendre, et aucune
      cible ne peut exercer le module tant que l'exemple ne tourne que contre
      l'émulateur.
